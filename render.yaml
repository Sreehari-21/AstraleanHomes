services:
  - type: web
    name: astralean-homes
    env: node
    region: oregon
    plan: free
    installCommand: npm install
    buildCommand: npm run build
    startCommand: npm run prod
    envVars:
      - key: NODE_ENV
        value: production

