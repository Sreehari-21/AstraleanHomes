services:
  - type: web
    name: astralean-homes
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run prod
    envVars:
      - key: ALLOWED_ORIGIN
        value: "*"
      - key: NODE_ENV
        value: "development"

